services:

  api:
    volumes:
      - ../../apps/api:/app
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8000

  frontend:
    volumes:
      - ../../apps/frontend:/app
    command: npm run dev -- --host 0.0.0.0

  worker:
    volumes:
      - ../../apps/workers:/app